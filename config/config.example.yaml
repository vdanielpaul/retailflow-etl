# Layered Configuration Example Reference for RetailFlow ETL Pipeline

extends: "base.yaml"

pipeline:
  environment: "development"
  batch_size: 5000

database:
  host: "localhost"
  port: 5432
  name: "retailflow_dw"
  user: "retailflow_user"
  password: "env_var:POSTGRES_PASSWORD"
