extends: "base.yaml"

pipeline:
  environment: "benchmark"
  batch_size: 10000
  max_error_percentage: 5.0

database:
  host: "localhost"
  port: 5432
  name: "retailflow_dw"
  user: "postgres"
  password: "env_var:POSTGRES_PASSWORD"
