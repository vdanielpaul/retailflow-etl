# Terraform and Google Cloud Provider Version Constraints
# Enforces compatibility across environments to prevent configuration drift.

terraform {
  required_version = ">= 1.5.0"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = ">= 5.0.0, < 6.0.0"
    }
  }
}
